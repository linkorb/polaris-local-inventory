stacks:
- name: lb
  chart: traefik
  host: swarm-manager
  tag: lb

- name: loki
  chart: loki
  host: swarm-manager
  tag: logging

- name: promtail
  chart: promtail
  host: swarm-manager
  tag: logging

# Our own apps
#
# Proxi - because it's easy
- name: proxi
  chart: proxi
  host: swarm-manager
  tag: app

# 3rd party apps we host
- name: whoami
  chart: whoami
  host: swarm-manager
  tag: app
