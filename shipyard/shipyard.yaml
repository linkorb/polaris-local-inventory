stacks:
- name: lb
  chart: traefik
  host: the-swarm-manager
  tag: lb

# Our own apps
#
# Proxi - because it's easy
- name: proxi
  chart: proxi
  host: the-swarm-manager
  tag: app

# 3rd party apps we host
- name: whoami
  chart: whoami
  host: the-swarm-manager
  tag: app
