ssh_pwauth: false

users:
- default # keep the "ubuntu" user, it's needed for `multipass shell`, etc
- name: ansible
  gecos: Ansible User
  groups: users,admin,wheel
  sudo:  ALL=(ALL) NOPASSWD:ALL
  shell: /bin/bash
  lock_passwd: true
  ssh_authorized_keys:
  - "{{ ssh_pubkey }}"
